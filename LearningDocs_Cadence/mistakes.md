# Mistakes & Misunderstandings Log

This file tracks misconceptions, incorrect explain-it-back answers, and prediction errors throughout the project. It serves as targeted review material every 5 chunks.

| Chunk # | What was misunderstood | Correction | Date |
|---------|------------------------|------------|------|
| 1 | M3 tonal elevation vs shadows, dynamic color fallback, and `Color.lerp` parameter `t` | M3 tints surfaces with primary color at higher elevations for better dark mode visibility; fallback uses a static seed `ColorScheme` when OS wallpaper palette is absent; `t` is the 0.0–1.0 progression factor between color `a` and color `b`. | 2026-09-06 |
| 2 | GitHub Actions `GITHUB_TOKEN` release write permissions, pipeline quality gates, and unique release tagging | `GITHUB_TOKEN` defaults to read-only and requires `permissions: contents: write` to publish GitHub Releases; `analyze` and `test` fail fast before slow APK builds; dynamic tags (like `v${{ github.run_number }}`) prevent git tag collisions when releasing on every commit. | 2026-09-06 |
| 3 | Offline UUID primary keys, SQLite JSON/array serialization, and Drift `get()` vs `watch()` | Client-side UUIDs prevent ID collisions across offline devices; SQLite stores arrays/json as serialized text via TypeConverters; `get()` returns a one-time Future whereas `watch()` returns a reactive Stream that auto-emits on any table changes. | 2026-09-06 |
| 4 | Supabase `anon` vs `service_role` keys, offline guest identity, and JWT access vs refresh tokens | `anon` key enforces Row Level Security (RLS) safely in client apps while `service_role` bypasses all security and must stay server-side; guest mode assigns a deterministic local UUID so offline users are never blocked; short-lived access tokens authenticate requests while refresh tokens renew them. | 2026-09-06 |
| 5 | Soft deletes vs hard deletes, Last-Write-Wins (LWW) timestamp comparison, and mid-sync network loss | Soft deletes (`is_deleted = true`) preserve deletion tombstones so drops propagate to remote databases rather than reviving on pull; LWW compares `updated_at` timestamps and retains the most recent version; mid-sync drops keep `is_synced = false` so local SQLite unsynced rows retry cleanly on reconnect. | 2026-09-06 |
| R1 | LWW remote-vs-local evaluation, tombstone propagation necessity, and client-generated UUID rationale | LWW strictly compares `updated_at` where remote wins only if strictly newer than local; hard deletes erase state and fail to sync deletions to remotes; client UUIDs guarantee global uniqueness without coordinating with an online auto-increment counter. | 2026-09-06 |
| 6 | Polymorphic schema consolidation, soft-delete propagation in feeds, and schema-less JSON metadata | Polymorphic `entries` consolidates syncing, indexing, and timeline feeds across telemetry; swiping feed items triggers `softDeleteEntry` so deletes replicate to cloud peers rather than reviving; JSON metadata accommodates novel attributes without SQL schema migrations. | 2026-09-06 |
| 7 | Dedicated finance schema rationale, Drift onUpgrade migration, and SQL aggregation vs in-memory sums | Dedicated `expenses`/`budgets` tables support relational budget joins, indexes, and typed currency aggregations; omitting `onUpgrade` crashes existing DBs with `no such table`; SQL `GROUP BY` and `SUM` processes aggregations in fast compiled C without marshaling thousands of rows into Dart memory. | 2026-09-06 |
| 8 | Clamping LinearProgressIndicator, reactive month state propagation, and empty-state donut chart guards | `LinearProgressIndicator` throws or distorts if `value > 1.0`, requiring `percentage.clamp(0.0, 1.0)`; mutating `selectedMonthProvider` reactively invalidates downstream Drift SQL streams without manual fetch calls; zero-spend months cause `0/0 = NaN` division, requiring explicit empty-state views. | 2026-09-06 |
| 9 | Riverpod stream-of-streams deadlock, dynamic streak calculation, and Drift stream test disposal | Nesting async stream transformations causes loading churn and test timeouts—reading `.value ?? []` synchronously produces instantaneous reactive derivation; calculating streaks dynamically avoids counter drift; unmounting widgets in Drift stream tests cleans up pending database query timers. | 2026-09-07 |
| 10 | Drift insertOnConflictUpdate vs partial update, SQLite DateTime serialization, and Riverpod 3 Notifiers | `insertOnConflictUpdate` requires all non-nullable columns without defaults in the Companion—use dedicated `(update(tbl)..where(...)).write(...)` for partial updates; Drift stores DateTimes as epoch seconds and reconstructs local DateTime—use `isAtSameMomentAs` for equality checks; Riverpod 3 replaces `StateProvider` with class-based `NotifierProvider`. | 2026-09-08 |
